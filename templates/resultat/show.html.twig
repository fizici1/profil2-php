{% extends 'base.html.twig' %}

{% block title %}Resultat{% endblock %}

{% block body %}
    <h1>Resultat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resultat.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ resultat.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resultat_index') }}">Retour à la liste</a>

    <a href="{{ path('resultat_edit', {'id': resultat.id}) }}">Modifier</a>

    {{ include('resultat/_delete_form.html.twig') }}
{% endblock %}
